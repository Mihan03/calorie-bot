spring:
  application:
    name: gateway 
telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
logging:
  pattern:
    level: "%5p [correlationId=%X{correlationId}]"
services:
  user-service:
    url: "http://localhost:8081/api/v1"